This folder contains backend-only utility scripts.

Current script:

- `setup-local-db.ts`: creates the local `app_users` table if needed and seeds
  the demo account `vetark / vetark4154`

Keep scripts here when they support local setup or maintenance but do not belong
inside a runtime module.
