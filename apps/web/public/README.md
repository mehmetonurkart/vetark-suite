This folder contains static frontend assets.

Current asset groups:

- `brand/`: Vetark login and profile visuals used by the login and dashboard
- `species/`: small animal icons used in dashboard summary cards

Keep reusable static images here so feature code can reference them by stable
public paths.
