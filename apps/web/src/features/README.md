Each frontend feature should live in its own folder under this directory.

Example:
- login/
- dashboard/
- appointments/

Keep components, hooks, forms, schemas, API clients, and tests related to the
same feature together. That way a feature can be changed or removed without
touching unrelated UI code.
