Each frontend feature should live in its own folder under this directory.

Current feature folders:

- `login/`: login UI, client API helpers, and session types
- `dashboard/`: authenticated home screen and visual dashboard layout

When a new domain is added, keep that domain inside its own folder with the
components, hooks, schemas, API helpers, and a short `README.md` it needs.
